server:
  port: 7000
spring:
  application:
    name: simple4h-gateway
  cloud:
    nacos:
      discovery:
        server-addr: nacos.cxm520hyq.com
nacos:
  DATA_ID: gateway-config
  GROUP: DEFAULT_GROUP
management:
  endpoints:
    web:
      exposure:
        include: '*'